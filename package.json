{
  "name": "noumenon",
  "version": "0.0.0",
  "private": true,
  "description": "Project Noumenon",
  "license": "MIT",
  "type": "module",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "check-types": "turbo run check-types",
    "format-and-lint": "biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "lefthook": "^1.11.0",
    "turbo": "^2.5.0",
    "typescript": "^5.8.0"
  },
  "trustedDependencies": ["@biomejs/biome", "lefthook"],
  "packageManager": "bun@1.3.4",
  "engines": {
    "bun": ">=1.3.4"
  }
}
